# Spring Boot API (browser calls this directly; CORS is enabled on the backend)
VITE_API_URL=http://127.0.0.1:5001

# Comma-separated; must match backend app.admin-emails for Moderation link
VITE_ADMIN_EMAILS=ajaylohith855@gmail.com
