# Built SPA: set full API origin (no /api suffix — client uses /api/... paths)
VITE_API_URL=https://your-production-api.example.com

# Must match backend app.admin-emails for the Admin link
VITE_ADMIN_EMAILS=ajaylohith855@gmail.com
